---
"@taskall/react": minor
---

TabMenu: `TabItem` aceita `content` opcional para o conteúdo do painel. Sem `content`, o painel continua exibindo o `label` (comportamento anterior).

Ícones: `iconSearchIndex` (nome, palavras-chave PT/EN e nome do componente) agora é exportado por `@taskall/react`.
